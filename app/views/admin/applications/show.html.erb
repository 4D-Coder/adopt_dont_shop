<section>
  <header>
    <h2>Applicant Information</h2>
  </header>

  <%@pets_show%>

  <p>Name: <%= @applicant.name %></p>
  <p>Address: <%= @applicant.address%> <%=@applicant.city%> <%=@applicant.state%> <%=@applicant.zip_code%></p>
  <p>Status: <%= @applicant.status %></p>
  <p>Description: <%= @applicant.description %></p>

  <p>Status of Application: <%= @applicant.status.gsub("_", " ")%>
</section>
<section>
  <%if @random == nil%>
  <h3>Add a Pet to this Application</h3>
  <%= form_with url: "/applications/#{@applicant.id}", method: :get, local: true do |form| %>
    <%= form.text_field :search %> 
    <%= form.hidden_field :applicant_id, value: @applicant.id%>
    <%= form.submit "Search" %>
  <% end %>
  <% end %>

  <% if @pets_show != nil%>  
  <h3> Pets Returned by Search </h3>
    <%@pets_show.each do |pet|%>
      <%= pet.name %>
      <%= button_to "Adopt this Pet", "/applications/#{@applicant.id}", params: {adopt: "#{pet.id}"}, method: :get %>
       <%# local: true %>
    <% end %>
  <% end %>



  <% if @adoptable_pets != nil %>
    <h3> Adoption Requests</h3>
      <% @adoptable_pets.each do |adopted| %>
        <%= adopted.name %> <br>
      <% end %>
    <% end %>


  <% if @adoptable_pets_2 != []%>

    <% @adoptable_pets_2.each do |adopted| %>

    <%relevant = ApplicationPet.where(application_id: @applicant.id).where(pet_id: adopted.id)%>  

    <% if relevant.first.approval == 0 %> 

    <h4>Pending pets you would like to adopt:</h4>

          <%= adopted.name %>

          <br>

          <%= button_to "Approve Application for #{adopted.name}", "/admin/application_pets/#{@applicant.id}", params: {pet_id: "#{adopted.id}"}, method: :patch %>



        <%elsif relevant.first.approval == 1%>
          <%="Application for #{adopted.name} is Approved."%>
        <%end%>

        <% end %> <%#-- corresponds to line 48 %>

    <% if @show_submission%>

      <h4> Submit your application</h4>

        <%= form_with url: "/applications/#{@applicant.id}", method: :get, local: true do |form| %>
        <%= form.text_area :description %> 
        <%= form.submit "Submit Description" %>
        <% end %>
      
      <% end %> 
    <% end %> 



    <%if @indicator != nil%>
      <p>Status of application: <%@applicant.status%></p>
    <%end %>
</section>